apiVersion: v1
kind: Service
metadata:
  name: mywebapp
spec:
  type: NodePort
  selector:
    app: mywebapp
  ports:
    - protocol: TCP
      port: 80              # The port exposed by the service
      targetPort: 80         # The port your container listens on
          
